#include "saml.h"


int solveSAML(int dimension, int board[dimension][dimension], int * * solution, int * size){
    // Now we try to solve the board
    
    // We will allocate solution and put a value to size
    if(solution == NULL || size == NULL){
        return 1;
    }
    
    
    
    
}